# Facebook Group Bot

## Setup

1. Put your Facebook `appstate.json` in this folder.
2. Edit `index.js` and replace:
   - `TARGET_THREAD_ID`
   - values in `NICKNAMES_MAP`
3. Install dependencies:
   `npm install`
4. Start:
   `npm start`

Never commit `appstate.json` to a public GitHub repository.
